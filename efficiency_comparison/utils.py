def linear_function(x, a=1, c=0):
    # y = a*x + c
    return a * x + c


def parabolic_function(x, a=1, b=0, c=0):
    # y = a*x^2 + b*x + c
    return a * x**2 + b * x + c


def hyperbolic_function(x, a=1, c=0):
    # y = a / x + c
    return (a / x) + c

